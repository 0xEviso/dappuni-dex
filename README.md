# Blockchain Masterclass from dapp university

Deploy and fill with placeholder data:

```shell
npx hardhat run --network localhost scripts/deploy.js
npx hardhat run --network localhost scripts/seed-exchange.js
```
